{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "sh -c \"export NPM_CONFIG_PRODUCTION=false && npm ci --omit=dev && cd client && npm ci --omit=dev && npm run build\""
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "sh -c \"npm run migrate:pg && npm start\"",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
