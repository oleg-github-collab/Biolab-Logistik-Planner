# 🤖 BL_Bot - Intelligent AI Assistant

BL_Bot є розумним AI-асистентом для Biolab Logistik Planner, який автоматизує комунікацію з командою та надає персоналізовану підтримку.

## 🎯 Основні Функції

### 1. Автоматичні Щоденні Нотифікації
- **Час**: Щодня о 8:00 (тільки робочі дні)
- **Зміст**:
  - Привітання з ранком
  - Кількість завдань в роботі
  - Кількість завдань до виконання
  - Попередження про прострочені завдання

### 2. Щотижневі Звіти
- **Час**: Щоп'ятниці о 15:00
- **Зміст**:
  - Статистика виконаних завдань за тиждень
  - Відпрацьовані години vs квота
  - Відкриті завдання
  - Події за тиждень
  - Персоналізовані поради

### 3. Розумні Запити з Природною Мовою
Користувачі можуть задавати питання боту і отримувати миттєві відповіді:

**Приклади запитів**:
- "Коли була моя остання відпустка?"
- "Покажи мої завдання"
- "Скільки годин я відпрацював цього тижня?"
- "Коли я останній раз був на лікарняному?"

### 4. Консультації з Бази Знань
BL_Bot має доступ до всієї бази знань і може:
- Відповідати на технічні питання
- Пояснювати процедури
- Надавати інструкції з безпеки
- Допомагати з хімікатами та обладнанням

## 🔧 Налаштування

### 1. Встановлення Залежностей

```bash
npm install openai node-cron
```

### 2. Налаштування OpenAI API

Додайте в `.env` файл:

```env
OPENAI_API_KEY=your_openai_api_key_here
```

### 3. Запуск Міграції

```bash
# Запустіть міграцію для перейменування бота
psql $DATABASE_URL -f server/migrations/050_rename_bot_to_bl_bot.sql
```

Ця міграція:
- Перейменовує EntsorgungsBot → BL_Bot
- Надає боту роль `admin`
- Оновлює тригери в базі даних

## 📊 Архітектура

### Компоненти Системи

```
server/
├── services/
│   ├── blBot.js           # Головний AI-сервіс
│   └── botScheduler.js    # Планувальник завдань
├── migrations/
│   └── 050_rename_bot_to_bl_bot.sql
└── routes/
    └── messages.pg.js     # Інтеграція в чат
```

### Потік Роботи

1. **Ініціалізація** (при старті сервера):
   - `botScheduler.start()` → ініціалізує `blBot`
   - Встановлюються cron-завдання для щоденних та щотижневих звітів

2. **Обробка Повідомлень**:
   ```
   User → Message → BL_Bot Detection → AI Processing → Response
   ```

3. **Scheduled Tasks**:
   ```
   Cron Schedule → Job Execution → Get Users → Send Notifications
   ```

## 🧠 AI Система

### System Prompt
BL_Bot використовує контекст користувача для персоналізованих відповідей:

- Інформація про користувача (роль, email, квота годин)
- Поточні завдання з пріоритетами
- Майбутні події
- Тижневий розклад
- Релевантні статті з бази знань

### Модель
- **Model**: GPT-4o
- **Temperature**: 0.7
- **Max Tokens**: 1000

## 💬 Спілкування з Ботом

### Через Чат
1. Відкрийте Messages в додатку
2. Знайдіть користувача "BL_Bot"
3. Надішліть повідомлення
4. Отримайте миттєву відповідь

### Автоматичні Нотифікації
Бот автоматично надсилає:
- Ранкові нотифікації (8:00, Пн-Пт)
- Тижневі звіти (15:00, П'ятниця)

## 🔍 Типи Запитів

### 1. Відпустки
```
"urlaub" / "відпустка"
→ Показує останні 5 відпусток
```

### 2. Лікарняні
```
"krank" / "лікарня"
→ Показує історію лікарняних
```

### 3. Завдання
```
"aufgabe" / "завдання"
→ Список відкритих завдань з пріоритетами
```

### 4. Робочі Години
```
"stunden" / "години" / "zeitplan"
→ Розклад тижня + статистика годин
```

### 5. AI-запити
Для всіх інших запитів використовується OpenAI для генерації відповіді на базі:
- Контексту користувача
- Бази знань
- Історії подій

## 📈 Статистика у Звітах

Щотижневий звіт включає:

1. **Виконані Завдання**: кількість завдань зі статусом "done" за тиждень
2. **Відпрацьовані Години**: реальні години vs квота
3. **Статус**: 📉 Під нормою / ✅ Норма / 📈 Понаднормово
4. **Відкриті Завдання**: поточний стан
5. **Події Тижня**: кількість календарних подій
6. **Поради**: персоналізовані рекомендації

## 🚀 API Методи

### BLBot Class

```javascript
const blBot = require('./services/blBot');

// Ініціалізація
await blBot.initialize();

// Обробка повідомлення
const response = await blBot.processIncomingMessage(userId, message);

// Відправка повідомлення
await blBot.sendMessage(userId, message);

// Ранкова нотифікація
await blBot.sendDailyMorningNotification(userId);

// Тижневий звіт
await blBot.sendWeeklyReport(userId);

// Генерація звіту (без відправки)
const report = await blBot.generateWeeklyReport(userId);
```

### BotScheduler Class

```javascript
const botScheduler = require('./services/botScheduler');

// Старт scheduler
await botScheduler.start();

// Стоп scheduler
botScheduler.stop();

// Ручний запуск (тестування)
await botScheduler.triggerDailyNotifications();
await botScheduler.triggerWeeklyReports();
```

## 🔐 Безпека

1. **Роль Адміна**: BL_Bot має роль `admin` для доступу до даних всіх користувачів
2. **System User**: Позначений як `is_system_user = true`
3. **API Key**: OpenAI ключ зберігається в змінних середовища
4. **Rate Limiting**: Використовується `setImmediate` для фонової обробки

## 🐛 Troubleshooting

### Bot не відповідає
1. Перевірте OPENAI_API_KEY в .env
2. Перегляньте логи: `grep "BL_Bot" logs/combined.log`
3. Перевірте статус бота в БД:
   ```sql
   SELECT * FROM users WHERE email = 'bl_bot@biolab.de';
   ```

### Scheduled tasks не працюють
1. Перевірте логи ініціалізації: `grep "BotScheduler" logs/combined.log`
2. Перевірте timezone: Europe/Berlin
3. Переконайтесь що сервер запущений

### AI відповіді некоректні
1. Перевірте model: повинен бути `gpt-4o`
2. Перевірте context: чи правильно збираються дані користувача
3. Налаштуйте system prompt в `blBot.js`

## 📝 Логування

BL_Bot логує всі важливі події:
- ✅ Успішна ініціалізація
- 📤 Відправлені повідомлення
- ❌ Помилки обробки
- 📊 Scheduled tasks execution

Перегляд логів:
```bash
# Всі логи бота
grep "BL_Bot" logs/combined.log

# Тільки помилки
grep "BL_Bot" logs/error.log

# Scheduler логи
grep "BotScheduler" logs/combined.log
```

## 🎨 Майбутні Покращення

- [ ] Підтримка голосових повідомлень
- [ ] Інтеграція з календарем Google
- [ ] Мультимовна підтримка (DE/EN/UA)
- [ ] Графіки та візуалізації у звітах
- [ ] Персоналізовані налаштування розкладу
- [ ] Інтеграція з Slack/Teams
- [ ] Machine learning для кращих рекомендацій

## 📞 Контакти

При виникненні проблем:
1. Перевірте документацію
2. Перегляньте логи
3. Створіть issue в репозиторії

---

**Версія**: 1.0.0
**Дата**: 2025-01-17
**Автор**: Claude AI Assistant
