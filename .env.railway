# ============================================
# RAILWAY PRODUCTION CONFIGURATION
# ============================================

# PostgreSQL (Railway автоматично додає DATABASE_URL)
DATABASE_URL=${{Postgres.DATABASE_URL}}

# Redis (опціонально)
# REDIS_URL=${{Redis.REDIS_URL}}

# JWT Secret
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Node Environment
NODE_ENV=production

# Server Port (Railway автоматично встановлює PORT)
PORT=5000
