[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[build.buildCommand]
buildArgs = { BUILDTIME_CACHEBUST = "$RAILWAY_GIT_COMMIT_SHA" }

[deploy]
startCommand = "node server-minimal.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
